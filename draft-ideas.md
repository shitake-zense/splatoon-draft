# draft-ideas.md — 新ドラフト機能 ブレインストーミング（案出し専用 / 未実装）

> このファイルは「案出し」のみ。実装はしない。
> 前提制約（既存仕様を壊さない方針）:
> - **無料枠で完結**（Firebase RTDB 無料枠・同時接続〜50・GitHub Pages 静的配信）。
> - **単一 `index.html` / Vanilla JS** 構成を維持。外部ライブラリ・CDN・実行時 fetch を増やさない。
> - Firebase スキーマは後方互換（追加はOK・破壊的変更NG）。
> - 既存武器エントリ（`key`/`name`/`cat`/`imgFile`/`sub`/`special`）は不変。
>
> 各案に **実装コスト感** を付与: ◎=低（CSS/JSローカル完結・DB増不要）, ○=中（同期/状態追加あり）, △=高 or 無料枠/構造に踏み込む。
> `sub`/`special` は既に `weapons.json` 全エントリに入っている資産 → これを使う案は低コストになりやすい。

---

## 1. ジャンルBAN系（構想の深掘り）

> 既存の「武器の絞り込み」は **ホストが事前に固定** する設定。これを **ドラフト中にプレイヤーが読み合いで切る BAN フェーズの行為** に拡張するのが肝。DEVELOPMENT.md の「既知の課題: BANモードの拡張」「今後の展望」に直結。

- **◎ 武器種BAN（カテゴリBAN）**: BANフェーズで個別武器ではなく「武器種を1枠まるごと」BAN。例: 相手にチャージャーを切らせない/切る。`cat` で一括除外するだけなので、絞り込みロジックを流用できる。
- **◎ サブBAN / スペシャルBAN**: 「ボム系」「ウルトラショット」など `sub`/`special` 単位でBAN。該当武器が全消去。**メタデータは既にあるので最も低コスト**。読み合いの幅が一気に増える（強スペ封じ＝エンジョイ層への配慮にも合致）。
- **○ 複合BAN枠**: 「武器BAN ×3 + ジャンルBAN ×1」のように粒度の違うBANを混在。ホストが各枠数を設定。
- **○ ジャンルBANの可視性ルール**: 武器種/サブ/スペシャルのどれをBAN対象に許可するかをホストがチェックで選ぶ（既存の絞り込みUIをBANフェーズ用に転用）。
- **意外性 △ ピンポイント解放（逆BAN/プロテクト）**: 「このカテゴリは絶対使いたい」を1つだけ宣言→相手はそれをBANできない。守りの駆け引き。
- **意外性 ○ ジャンルBANのドラフト（交互に切る）**: ジャンルBANだけを交互に数ラウンド→残ったプールで通常ピック。MOBAのドラフト感が出る。

---

## 2. 新しいピック / ドラフト形式

- **○ ミラー禁止 / 強制ミラー**:
  - *ミラー禁止*: 両チームで同一武器ピック不可（同時ピックの「被りOK」の反対オプション）。
  - *カテゴリミラー*: 相手が選んだ武器と同じ武器種で別武器を選ぶ縛り。
- **○ カウンターピック公開制**: 相手の直前ピックを見てから選ぶ（スネーク系の延長だが「相手の手が見えた状態で1枚返す」ターンを明示）。
- **△ オークション/入札ピック**: 各チームにポイント。武器に入札し高い方が獲得、ポイント消費。強武器は高い。`pending_*` 同様の同期で実現可だが状態管理は重め。**戦略性は最高クラス**。
- **○ ガチャ/運命ピック**: ランダムで武器が配られ、各チーム数回だけ「引き直し（リロール）」可。運要素のパーティ寄り。`pickRandom` 資産を流用。
- **○ ドラフト後トレード相**: ピック完了後に「この1枚を相手の1枚と交換しませんか？」を提案→相手承認で成立。和やかな調整 or 心理戦。
- **意外性 ○ スティール（強奪）**: 1試合に1回、相手が確定した武器を奪って自分のものにできる（相手はリピック）。1回限りの大技。
- **意外性 △ ブラインド入れ替え**: 全ピック後、各チーム1枚を伏せたまま選び一斉公開で確定（最後にもう一段の読み合い）。

---

## 3. 制約・お題・ハンデ系（バランス＆ネタ）

- **◎ お題カード / チャレンジドラフト**: 開始時にランダムなお題を提示。例:「全員サブがボム」「チャージャーを1人必ず入れる」「スペシャル被り禁止」「全員違う武器種」。`cat`/`sub`/`special` で判定可能。守れているかをUIで○×表示。**パーティ性が高く低コスト**。（実装済み：Phase 2／ピック中の常時バナーも追加済み）
  - **△ 強制力版（リアルタイムに選べる武器が減る）**: ◎の非強制版とは別物で**△（高・ほぼ1フェーズ規模）**。1手ごとの「達成可能性」増分判定＋タイマー自動補完/enemy/同時ピックへの波及＋BANとの組合せでの実現不能(デッドロック)回避が要る。詳細見積りは ROADMAP.md「Phase 2 → お題に強制力を持たせる場合のコスト分析」。
- **◎ 縛りルーレット**: 開始前にスロット/ルーレット演出で縛りを抽選（お題カードの演出強化版）。
- **○ ハンデ機能**: 実力差調整。弱い側に「先攻固定」「BAN+1」「リロール権」などを付与。設定で選択。
- **意外性 ○ 封印解除ボーナス**: BANされた武器のうち1つを、劣勢側が「復活」させて使える特権。
- **◎ コンプ警告（バランスサジェスト）**: 「シューターに偏っています」等を非強制で表示。初心者の編成補助＝コンセプト「初心者にもチャンス」に合致。

---

## 4. シリーズ / 連戦モード（既存資産で映える本命候補）

- **○ BO3 / BO5 連戦モード**: 1ルームで複数試合を連続実施。試合間でステージ/ルールが自動で変わる。結果を試合ごとに保持。
- **意外性 ○ 武器ロックアウト（プール枯渇制）**: シリーズ中、**一度ピックした武器は次の試合で使えない**。BO3後半ほど手札が枯れていく独特のメタ。スプラ特有の面白さが出る、かなり新規性のある案。
- **○ 勝者ペナルティ / 敗者特権**: 前の試合の勝者は次戦でBAN-1、敗者は先攻、など連戦の揺り戻し。
- **△ シリーズ戦績ボード**: 何戦何勝、各試合の編成一覧を1画面に。結果PNGの「シリーズ版」も生成。

---

## 5. 共有・観戦・履歴（DBを増やさず無料枠を守る工夫）

> ここは「**DBに書かずクライアント完結**」を徹底すると無料枠を一切圧迫しない。意外性と実用性のバランスが良い領域。

- **◎ 結果の共有コード/ディープリンク**: 完成ドラフトを **URLハッシュ or 文字列にエンコード**（DB不要）。開くと結果画面を即再現。SNS導線（結果PNGと相性◎）。
- **◎ ドラフト履歴（ローカル）**: `localStorage` に完了ドラフトを保存し「過去のドラフト一覧」から再表示/再PNG化。**再参加で既に localStorage を使っている**ので延長で実装可。
- **◎ リプレイ（ステップ再生）**: 保存したドラフトをBAN→PICK順に1手ずつ再生する閲覧モード。配信・解説向き。
- **○ 観戦モード（read-only）**: `?room=XXXX&spectate=1` で参加せず購読のみ。RTDB読み取り増のみで書き込み増なし。
- **意外性 ◎ QR共有**: 結果/ルームURLをクライアント側でQR化（軽量な自前描画 or 既存canvas活用）。対面で即共有。
- **意外性 ○ 配信オーバーレイモード**: 結果/進行を大きく高コントラストで表示する「OBS取り込み用」レイアウト（既存DOMのCSSテーマ切替で実現）。

---

## 6. データ活用（`sub`/`special` の既存資産を活かす）

- **◎ グリッド内フィルタ/検索**: ピック画面でサブ・スペシャル・名前で絞り込み・インクリメンタル検索。武器数が多い今、UX改善効果大。
- **◎ サブ/スペシャル別表示・ソート**: グリッドをカテゴリ以外の軸でも並べ替え。
- **○ 「おまかせ編成」提案**: 武器種が散るようランダム提案（バランス重視のワンクリック）。
- **○ 編成サマリー**: 結果画面に「サブ内訳/スペシャル内訳/武器種内訳」の小さな統計を表示。結果PNGにも反映可。
- **△ 簡易ティア/重み付け**: コミュニティ感覚の強さタグを `weapons.json` に任意追加（既存フィールド不変・追加のみ）。お題やオークションの土台になる。

---

## 7. 演出・パーティ・意外性（“読み合いの楽しさ”を増幅）

- **◎ ピック確定演出・SE**: 確定時のアニメ/効果音（Web Audio で自前生成すれば外部ファイル不要）。ドラフトの「決まった！」感を強化。
- **◎ カウントダウン演出強化**: 残り時間でグリッドが赤く脈動など、緊張感の可視化。
- **意外性 ○ 軽量リアクション/煽りスタンプ**: ドラフト中に絵文字スタンプを飛ばす（RTDBの揮発フィールドに一瞬書いて消す）。身内エンジョイに刺さる。
- **意外性 ○ 罰ゲームルーレット**: 敗者用のお題/罰をルーレット抽選（完全クライアント完結のネタ機能）。
- **意外性 △ “神の手”（ホスト特権）**: ホストが1回だけ任意の武器を強制差し替え/没収できるカオスモード。身内専用の遊び。
- **◎ 武器読み上げ（TTS）**: Web Speech API で確定武器を読み上げ（アクセシビリティ＋実況感、外部依存なし）。

---

## 8. UX / アクセシビリティ / 国際化

- **◎ ダーク/ライト・色覚配慮テーマ**: アルファ/ブラボー色のコントラスト見直しとテーマ切替。
- **◎ 直前操作のUndo（自分のターン内）**: 誤タップ救済（確定前まで）。
- **○ 多言語(EN)対応**: 海外スプラ勢向け。文字列を1オブジェクトに集約すれば単一ファイルのまま可能（D7状態集約の副産物にもなる）。
- **◎ チュートリアル/ヘルプのアプリ内表示**: 初見にPICK順7種が複雑問題（DEVELOPMENT.md記載）への対処。各モードに「？」で説明ポップ。
- **◎ 設定プリセット保存**: よく使うルール設定を localStorage に名前付き保存し1クリック復元。

---

## 9. 今は見送り（無料枠/構造を超えるため要検討メモ）

- **△ アカウント/レーティング/戦績の永続化**: 個人戦績・ランクは認証強化やDB増が必要 → 無料枠と「身内向け」設計を超える。やるなら別構想。
- **△ マッチングシステム / Discord連携**: DEVELOPMENT.md の「今後の展望」。常時稼働サーバ前提になりがち＝無料枠外。
- **△ スケール対応（同時接続>50）**: 別バックエンドの検討が必要。

---

## おすすめ優先順（“低コスト × 効果/意外性”で抜粋）

1. **サブ/スペシャルBAN（§1）** — メタデータ既存で低コスト、構想「ジャンルBAN」の本命、戦略性◎。
2. **お題/チャレンジドラフト（§3）** — パーティ性が高く既存データで判定可、低コスト。
3. **結果の共有コード/履歴/リプレイ（§5）** — DBを増やさず無料枠を守りつつSNS導線とリプレイ価値。
4. **グリッド内検索/フィルタ（§6）** — 武器増加に伴うUX負債の解消、地味だが効く。
5. **武器ロックアウト付きBO3連戦（§4）** — 新規性が高くスプラ特有の面白さ。中コストだが“目玉”候補。
