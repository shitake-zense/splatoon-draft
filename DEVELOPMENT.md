# 開発経過

このドキュメントは、splatoon-draft の開発における設計判断・トレードオフ・既知の課題・今後の展望をまとめたものです。機能や使い方そのものは [README.md](./README.md) を参照してください。ここでは「なぜそう作ったのか」「何を諦めたのか」を正直に残すことを目的としています。

---

## 開発の背景・動機

- 対戦ゲームでは編成や構成の相性が勝敗を大きく左右することがあり、試合開始まで相手構成が分からないと、相性面で一方的な展開になりがちなケースがある。
- 試合前に BAN と PICK を行うドラフト方式を導入すれば、その相性問題を事前に調整できないかと考えて開発を始めた。
- 着想元は 3v3 対戦ゲーム「ブロスタ」のガチバトル（1-2-2-1 固定のピック順）。これをスプラトゥーン3の 4v4 に応用しようとした。
- 4v4 ではどの順番が最もバランスが取れるのか一意に決まらないため、ピック順のバリエーションは自分で複数案を考案して試行錯誤した。
- 友達とエンジョイで遊ぶ中で実際に使い、ピック・BAN の読み合いそのものが楽しさになることも分かってきた。

---

## 設計判断とトレードオフ

### 単一 HTML ファイル / Vanilla JS 構成
- フレームワークを導入せず `index.html` 一枚にまとめている。
- 理由①: 個人開発として無料枠で完結させたかった。
- 理由②: AI にコーディングを任せる際、単一ファイルの方がコンテキスト消費を抑えられて取り回しが良い。
- トレードオフ: 規模が大きくなった際の保守性は犠牲にしている。現状は 1 ファイルで全体を把握できる規模なので許容している。

### Firebase Realtime Database の採用
- 小規模の身内対戦が想定範囲で、リアルタイムな状態同期が要件だった。
- GitHub Pages ホスティングと相性が良く、サーバー運用不要で実現できる。
- ※ Firestore と RTDB の細かな違いについては、開発時点で深く比較検討したわけではない。今回の用途では RTDB で要件を満たせたため採用したというのが正直なところで、より適した選択がないかは今後の改善余地として継続的に見直したい。

### PICK モードを7種類用意した理由
- 4v4 というスプラトゥーンの形式では「最もバランスが取れるピック順」が一意に決まらないため、複数案を比較できるようにした。
- スネーク・クラシックスネーク・ミドルスネーク・バランス・前重ね・交互・同時ピックの7種類は、いずれも自分でバランスを考案・調整したもの。
- ユーザーが飽きずに遊べる多様性を重視した。
- 個人的には、スネークと同時ピックが競技性とエンジョイ性の両立という意味で気に入っている。
- トレードオフ: 選択肢が増えるぶん、初見ユーザーには UI が複雑に見える可能性がある。

### 「相手ピック」モードの発想と実装
- 友達とのエンジョイ対戦中、「相手の武器を自分が決めてみたら面白いんじゃないか」と冗談半分で始めたところ、普段使わない武器を持たされる面白さや、BAN と組み合わせた時の戦略性が予想以上に盛り上がった。
- これは面白いと確信して機能として実装した。
- 実装で苦労した点: ピック順で対応する選手同士をペアリングするロジックを AI が一発で正しく組めず、対応付けの仕様（アルファ[N] ↔ ブラボー[N]）を丁寧に説明する必要があった。
- 結果として、同時ピックとの併用も可能な形で実装できている。

### 武器の絞り込み機能を入れた理由
- 機能の幅を持たせる狙いに加え、「初心者にもチャンスがある構造」を作りたかった。
- 例: 強いスペシャルを除外することで、ガチ寄りプレイヤーとエンジョイ層が同じ土俵で楽しめる。
- 「ガチ用にもエンジョイ用にも使える」を目指したコンセプトの一部。

### ルーム自動削除を GitHub Actions で実装した理由
- 動作はできるだけ軽く保ちたかった（古いデータが残り続けるのを避けたい）。
- GitHub 内で定期実行できる仕組みがあることを知り、外部の常時稼働サーバーを用意せずに済む点が魅力だった。
- 結果として、毎日 0 時（JST）に古いルームを掃除する運用を、追加コストゼロで実現できている。

### 結果画像 PNG 生成を端末内で完結させた理由
- SNS でのシェアしやすさを意識した機能。
- 画像生成は技術的に挑戦してみたい領域でもあった。
- 生成処理を外部 API に送らず端末内で完結させているのは、ユーザーのプレイ情報を不要に外部送信しない方が望ましいと考えたため。

---

## 既知の課題

現状は身内利用を前提に「動くものを作る」ことを優先した結果、以下が未対応です。

- **同時接続数の制約**: Firebase 無料枠の同時接続上限（約 50）に依存しているため、利用者が増えた場合のスケールには対応していない。
- **BAN モードの拡張余地**: 現在は武器単位の BAN のみ。武器種・サブウェポン・スペシャル単位での BAN にも面白さがあると考えているが、未実装。
- **荒らし対策の手薄さ**: もともと身内利用を前提に設計したため、悪意のあるユーザーへの対策はごく一部しか入っていない。広く公開する場合は要対応。

---

## 今後の展望

無限に時間とお金があるならば、という前提での構想を含む。

- スケールに耐えられる構成へ移行し、広く使ってもらえる形にしたい。
- BAN モードの拡張（武器種・サブ・スペシャル単位）。
- Discord サーバーの運営とマッチングシステムの構築まで踏み込めれば、スプラトゥーンのコミュニティそのものを盛り上げる足がかりになると考えている。

---

## 開発の振り返り

- 開発期間は累計で約 1 ヶ月（連続ではなく断続的）、コミット数は 87。
- 初期の土台作り、特に data（武器データ）の整備に手間取った。
- 自分の頭の中にあった形が徐々に動くものになっていく過程が、一番楽しかった。
